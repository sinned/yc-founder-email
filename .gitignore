# Personal contact data — never commit
Connections.csv
*Connections*.csv
connections*.csv

# ...but keep the dry-run fixture (it contains no real contacts)
!sample-connections.csv

# File-based CRM: live data holds real contact info + outreach status.
# Repo is public, so keep real records and the board local. Commit only the
# system: README.md, _template.md, and the fake contacts/_example.md.
crm/contacts/*
!crm/contacts/_example.md
crm/pipeline.md
